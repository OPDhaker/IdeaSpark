/**
 * Invite to the event WhatsApp group — where organisers run day-of
 * coordination. A constant, not an `event_config` column: `/admin/event` has no
 * form for link columns yet, so a column would only ever be settable from the
 * seed script.
 */
export const WHATSAPP_INVITE_URL =
  "https://chat.whatsapp.com/FS2AgVUZUoe4hFroL17qpk?mode=gi_t";
